../usecase/users.go
```
package usecase

import (
	"main/dao"
	"main/model"
	"errors"
	"github.com/google/uuid"
)

func GetUsers() ([]model.User, error) {
	users, err := dao.GetUsers()
	if err != nil {
		return nil, errors.New("Failed to get users")
	}
	return users, nil
}

func GetUser(id string) (model.User, error) {
	user, err := dao.GetUser(id)
	if err != nil {
		return model.User{}, errors.New("Failed to get user")
	}
	return user, nil
}

func CreateUser(user *model.User) error {
	user.ID = uuid.New().String()
	err := dao.CreateUser(user)
	if err != nil {
		return errors.New("Failed to create user")
	}
	return nil
}

func UpdateUser(user *model.User) error {
	err := dao.UpdateUser(user)
	if err != nil {
		return errors.New("Failed to update user")
	}
	return nil
}

func DeleteUser(id string) error {
	err := dao.DeleteUser(id)
	if err != nil {
		return errors.New("Failed to delete user")
	}
	return nil
}
```

../usecase/users.go
```
package usecase

import (
	"main/dao"
	"main/model"
	"errors"
	"github.com/google/uuid"
)

func GetUsers() ([]model.User, error) {
	users, err := dao.GetUsers()
	if err != nil {
		return nil, errors.New("Failed to get users")
	}
	return users, nil
}

func GetUser(id string) (model.User, error) {
	user, err := dao.GetUser(id)
	if err != nil {
		return model.User{}, errors.New("Failed to get user")
	}
	return user, nil
}

func CreateUser(user *model.User) error {
	user.ID = uuid.New().String()
	err := dao.CreateUser(user)
	if err != nil {
		return errors.New("Failed to create user")
	}
	return nil
}

func UpdateUser(user *model.User) error {
	err := dao.UpdateUser(user)
	if err != nil {
		return errors.New("Failed to update user")
	}
	return nil
}

func DeleteUser(id string) error {
	err := dao.DeleteUser(id)
	if err != nil {
		return errors.New("Failed to delete user")
	}
	return nil
}
```

Since there are no changes needed in the controller, there is no new code to generate. The existing code in the controller does not need to be modified to implement the new feature.

Since there are no changes needed in the model, there is no new code to generate. The existing code in the model does not need to be modified to implement the new feature.

Since there are no changes needed in the main, there is no new code to generate. The existing code in the main does not need to be modified to implement the new feature.
1. dao package
   - Add a function in users.go to check if a user exists by their ID.

2. usecase package
   - Modify the CreatePost function in posts.go to call the new function from the dao package to check if the user exists before creating a new post.

3. controller package
   - Modify the CreatePost function in posts.go to handle the error if the user does not exist when creating a new post.